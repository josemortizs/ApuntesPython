# Trabajo con TUPLAS:

tupla = ()
tupla = ("a", 1, True)
# Con las tuplas se pueden usar:
# for, in, not in, +, slice, len(tupla)...

# LAS TUPLAS SON INMUTABLES
# Por lo tanto: no hay append, remove, etc
# Sí tiene: tupla.index(), tupla.count(), etc..